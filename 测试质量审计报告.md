# Momentum 测试质量审计报告

更新日期：2026-07-14

## 结论

本轮改造把测试可信度从“数量很多，但关键错误仍可能漏过”的约 5/10，提升到约 8/10。最重要的变化不是增加测试数量，而是让覆盖率口径、存储边界和变异门禁都运行真实生产代码，并让旧报告、恒真断言和 Mock SUT 无法通过 required CI。

当前仍不能宣称已验证真实 Supabase 数据库语义。新增的 Supabase 集成测试运行真实 `SupabaseStorage`、API 模块、mapper 和 `@supabase/supabase-js`，但 HTTP 边界由有状态 MSW 提供，因此不覆盖 RLS、migration、PostgreSQL 约束或 RPC 函数语义。

## 修复前后数据

| 指标                            |                         修复前 | 修复后（保守复跑值） |
| ------------------------------- | -----------------------------: | -------------------: |
| Coverage project 测试文件       |                            222 |                  239 |
| Coverage project 测试数         |                          1,736 |                1,790 |
| Statements                      |       71.96%（全源码真实基线） |               74.96% |
| Branches                        |       62.42%（全源码真实基线） |                65.5% |
| Functions                       |       73.30%（全源码真实基线） |               75.74% |
| Lines                           |       72.82%（全源码真实基线） |               75.74% |
| 未进入 coverage JSON 的生产文件 |                            147 |                    0 |
| Critical mutation 总分          | 两个模块分别为 66.67% / 69.11% |               96.97% |
| Critical mutation NoCoverage    |                       未设门禁 |                    0 |

旧的 84.37% statements / 74.35% branches 只统计被测试导入的文件，不能代表仓库真实覆盖率。新口径显式包含全部 `src/**/*.{ts,tsx}` 生产源文件，未执行的文件按 0% 计入。

## 已完成的可信度改造

### 1. 覆盖率与测试运行器

- 新增统一 coverage 配置，在一次 Vitest 运行中分别执行 unit 和 integration project，并使用各自 setup。
- 固定 JSDOM URL，使用原生且相互独立的 `localStorage` / `sessionStorage`；每个测试后分别清理。
- 集成 setup 不再全局启用 fake timers，时间控制改为测试显式 opt-in。
- Coverage 先写临时目录，只有测试和阈值全部成功后才替换正式报告。
- Coverage metadata 记录 HEAD、配置哈希、工作树哈希和生成时间。热点分析拒绝缺失、配置不匹配或源码/测试已变化的报告。
- 全局阈值为 statements 74%、branches 65%、functions 75%、lines 75%；include 范围没有为达标而缩小。

### 2. 真实存储边界

- `MomentumStorage.contract.test.ts` 只运行真实 `localStorageAdapter`，删除测试内部的 `InMemoryStorage` 和重复契约。
- 本地契约覆盖 Chain、Session、History、Pet、Task Time Stats，以及 RSIP nodes、meta、groups、policy library、run history、task links 和 execution records 的保存、追加、更新、删除语义。
- 删除基于测试目录 Map 的伪 DB suite、配置、setup 和 `test:db` 脚本。
- 新增 Supabase HTTP 边界集成测试，真实运行 storage、API、mapper 和 Supabase SDK；只替换外部 HTTP。
- 覆盖未认证行为、Chain 日期映射与幂等 upsert、replacement 删除、软删除/恢复/永久删除、ScheduledSession 更新与移除、CompletionHistory 顺序与替换，以及真实传输错误传播。
- 保留并发读去重、失败后重置、写操作不去重和迁移编排测试；删除只证明 mocked proxy 被调用的用例。

这组测试揭示并修复了一处生产缺陷：Supabase `saveCompletionHistory` 现在具有替换语义，而 `appendCompletionHistory` 保持追加语义。

### 3. 弱测试与过度 Mock

- 删除伪造墙钟性能、`expect(true).toBe(true)` 类恒真测试和替换 optimizer 后再验证“恢复”的用例；本阶段不建立不稳定的墙钟性能门禁。
- `usePetWidgetController` 直接覆盖并发喂食、成功/失败反馈、500ms 收养定时器、鼠标/触摸拖动、RAF 合并、边界裁剪、持久化和卸载清理。
- `useGroupCardScheduleCountdown` 覆盖无计划、单次预警、到期失败、计划切换重置和 interval 清理，并修复重复通知风险。
- `GroupViewContainer` 通过真实 View 和真实重复次数弹窗完成“输入 6、保存、收到 6、关闭弹窗”的用户路径。
- CacheCore、petLogic、usePetDomain 与 RuleSelectionDialog 均改为精确行为断言；目标 RuleSelectionDialog 用例已连续运行 10 次通过。
- 全局启用 `vitest/expect-expect` 和 `vitest/no-conditional-expect`。Result 分支使用 `expectOk` / `expectErr` 收窄，不再把断言藏在条件分支中。
- AST 审计禁止 literal 恒真、模糊 truthy/falsy、自比较，以及测试文件直接 Mock 同名 SUT 模块。

### 4. 故障揭示门禁

- Session scheduling 测试覆盖非首项目标、多 chain/schedule、重复与缺失目标、精确到期时间、revision `+1`、remove/save 拒绝路径。
- RSIP view interaction rules 覆盖错误 group/node、非首项目标、reinforcement `undefined/0/1`、非目标保持不变、精确累计值，以及 E0 6/7、E1 20/21 边界。
- `test:mutation:critical` 当前结果：198 mutants，192 killed，6 survived，0 NoCoverage；`scheduling.ts` 为 100%，`viewInteractionRules.ts` 为 95.12%，均高于每文件 84% 门槛。
- Nightly mutation 使用语义目录范围；新增 sessions/RSIP 文件必须被匹配，或登记原因、负责人和到期日。
- Mutation JSON 绑定 HEAD、完整工作树、配置、scope、报告哈希和 Stryker 版本；缺失或过期报告在 required 模式下失败。

## 剩余风险与下一阶段

全部生产文件现在都会进入 coverage JSON，但仍有 85 个含可执行语句的生产文件为 0%。当前高风险热点包括：

- `src/components/useTaskGroupEditor.ts`
- `src/components/focus-mode/hooks/useExceptionRuleOperations.ts`
- `src/components/focus-mode/hooks/useFocusTimers.ts`
- `src/components/rsip/hooks/useRSIPReparent.ts`
- `src/components/rsip/hooks/useRSIPViewInteractionActions.ts`
- `src/components/task-completion-dialog/hooks/useTaskCompletionDialog.ts`
- `src/components/recycle-bin-modal/hooks/useRecycleBinModal.ts`

这些文件已进入可见的 coverage 热点报告，不会再因“未被导入”而消失。后续应优先补 Focus、Recycle Bin 和其余 RSIP controller 的故障揭示测试，而不是单纯追求总覆盖率数字。

本轮明确未验证：

- 真实 Supabase RLS、migration、数据库约束和 RPC 语义；
- SQL/pgTAP；
- Rust 覆盖率；
- 真实浏览器性能基准；
- 完整 nightly mutation 的所有存活变异分析。

## CI 结果口径

`quality:ci:required` 现在包含测试 lint、AST 断言/Mock 审计、统一真实 coverage、coverage 新鲜度/热点检查和 critical mutation。Integration suite 已包含在 coverage project 中，因此 required lane 不再重复单独执行它。

报告与门禁只表示本报告列明的边界；MSW 集成测试不得被描述为数据库、RLS 或 migration 测试。
