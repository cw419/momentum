# Momentum 前端改进计划

> 核对基准：2026-07-13 当前工作区。本文把已验证事实、待验证假设和实施建议分开，避免用文件名、行数或理论性能收益代替真实的用户问题。

## 目标

前端改进应优先带来以下结果：

1. 键盘、触屏和小屏用户可以稳定完成关键流程。
2. 样式规则有清晰所有权，不再依赖全局覆盖和补丁叠加。
3. 界面层级服务于任务流，而不是重复堆叠卡片、玻璃效果和装饰性标题。
4. i18n、对话框和响应式行为具备可自动验证的约束。
5. 架构或框架升级必须有测量结果，不以“理论上更快/更小”作为完成标准。

## Fact check

### 1. CSS 文件碎片化：结论部分成立，数字和处置方式需修正

- `src/styles/` 当前确有 7 个 CSS 文件，但总计是 **2,224 行**，不是 2,483 行。
- 入口实际加载：
  - `mobile-optimizations.css`，并由它导入 `mobile-visual-fix.css`
  - `mobile-touch-optimization.css`
  - `rsip-canvas.css`
- `layout-fixes.css`、`responsive-layout.css`、`slider-responsive.css` 当前没有静态导入路径。它们首先是待确认并删除的死文件，而不是待合并文件。
- 已加载的自定义 CSS 连同 `src/index.css` 约 1,477 行，包含 33 处 `!important`，以及针对 `button`、表单控件和通用卡片类的宽泛覆盖。真正风险是全局规则的影响范围和层叠冲突，不只是文件数量。
- `touch-action`、`overscroll-behavior`、安全区、复杂伪元素和输入设备 media query 不适合一律改写成组件内 Tailwind 类。

因此，正确方向是“删除死文件 + 按所有权拆分规则 + 收窄全局选择器”，而不是先把所有内容合并到一个大文件。

### 2. i18n：问题成立，但现有基础比原描述完整

- `src/i18n/translations.ts` 当前每种语言有 6 个 key。
- 系统已经同时提供 `t(key, params)` 和 `tr(zh, en)`；不是只有内联双参数模式。
- 静态扫描显示约 163 个源码文件仍调用 `tr(...)`，因此第三语言扩展、翻译审校和遗漏检查确实困难。
- 当前 `t` 的 key 类型仍是普通 `string`，`translations` 也被声明为 `Record<string, string>`，所以 TypeScript 不能检查拼错的 key。
- `i18next-extract` 只适合在决定采用 i18next 生态后使用；当前自研实现更适合先加类型约束和仓库内检查脚本。

应渐进迁移，禁止一次性替换上千个调用点。key 应按稳定的业务语义命名，例如 `common.actions.close`、`dashboard.empty.title`，而不是机械绑定组件文件名。

### 3. 大文件：原清单已经过时

原文列出的文件当前行数如下：

| 文件                                        | 当前行数 | 状态                          |
| ------------------------------------------- | -------: | ----------------------------- |
| `src/components/Dashboard.tsx`              |       11 | 已是 Controller/View 门面     |
| `src/components/AccountModal.tsx`           |      150 | 已拆出 controller 和 sections |
| `src/components/ImportExportModalParts.tsx` |       12 | 已变为导出门面                |
| `src/components/rsip/RSIPTaskLinkPanel.tsx` |       97 | 已低于阈值                    |

`CLAUDE.md` 写的是“目标小于 300 行”，不是单文件硬限制。真实 CI gate 是 ratchet：不得新增超过 300 行的文件，也不得让基线中的超限文件继续增长。当前 gate 通过，仍有 5 个基线文件超过 300 行，其中前端相关的是：

- `src/components/ImportUnitsModal.tsx`：308 行
- `src/components/chain-editor/sections/MainChainSettingsSection.tsx`：308 行
- `src/app/app-shell/AppShellView.tsx`：307 行

这些文件只比阈值多 7–8 行，不应为了数字做无意义拆分。只有在职责、可测试性或复用边界同时改善时才拆。

### 4. 对话框基础设施：方向成立，但不能直接推导为“全面引入 shadcn/ui”

- 代码里已经存在 `useFocusTrap`、`Portal`、`role="dialog"`、`aria-modal` 和部分 Escape 处理，并非完全没有无障碍基础。
- 实现并不一致：扫描到 8 个带 `role="dialog"` 的渲染点，而共享 `useFocusTrap` 只在 3 个组件中使用；Escape、初始焦点、关闭后的焦点恢复、背景 inert/滚动锁定和嵌套对话框也分散处理。
- 当前 `useFocusTrap` 能循环 Tab 并恢复焦点，但不负责 Escape、背景 inert、滚动锁定或 Portal。

因此应先建立统一的 Dialog 契约，再通过一个限时 spike 比较“内部 `DialogShell`”与 [Radix Dialog](https://www.radix-ui.com/primitives/docs/components/dialog)。Radix 原生覆盖焦点陷阱、Escape、焦点恢复和 WAI-ARIA 模式；shadcn/ui 还会引入一套组件样式和生成代码，不应与 Radix Primitives 混为同一个决策。

### 5. Zustand 订阅：现象仍在，但原位置和优先级不准确

- 两个 `useAppShellStore(useShallow(...))` 已从 `AppShellContainer.tsx` 移到 `src/app/app-shell/useAppShellState.ts`。
- 两个 selector 在 store 更新时各执行一次，但当前只是读取字段并创建小对象，没有证据表明它是渲染热点。
- 合并 selector 不一定减少组件重渲染；拆成两个 store 则会增加状态同步、reset 和持久化复杂度。

这条不应进入主动重构队列。先用 React Profiler 或针对性的 render-count 测试证明问题，再选择原子 selector、派生 view model 或 store 拆分。

### 6. Tailwind v4：版本判断正确，收益描述不准确

- 项目当前是 Tailwind `3.4.19`、Vite `7.3.1`；Tailwind v4 已发布，并提供官方 Vite 插件和 CSS-first 配置。
- 官方数据支持编译和增量构建更快，但不保证项目的最终 CSS、Web bundle 或 Tauri 安装包一定更小。
- 迁移不只涉及 `@apply`：还包括 `@tailwind` 指令、PostCSS/Vite 插件、JavaScript 配置加载、Preflight、ring/shadow/outline 默认值、自定义 `@layer` utility、浏览器基线等变化。
- Tailwind v4 的官方浏览器基线是 Safari 16.4+、Chrome 111+、Firefox 128+。Tauri/WebView 与 Web 端支持范围必须先确认。

升级应遵循 [Tailwind v4 upgrade guide](https://tailwindcss.com/docs/upgrade-guide)，作为独立 spike 评估，不能和 CSS 清理混在同一个 PR。

## 改进后的实施计划

### Phase 0：建立前端回归基线（P0）

先覆盖最重要的用户路径：Dashboard 浏览与启动、链条创建/编辑、Focus Mode、任务完成、数据导入导出、规则选择和回收箱。

实施内容：

- 固定桌面、小屏手机、横屏和触屏/粗指针四类验证环境。
- 为关键路径记录当前截图、键盘顺序、焦点归属和构建产物 CSS 大小。
- 明确 light/dark、200% zoom、`prefers-reduced-motion` 和长中英文文案的检查矩阵。
- 现有 Testing Library 行为测试继续作为逻辑基线；视觉回归工具是否引入，单独做轻量选型。

验收标准：后续每个前端 PR 都能说明影响了哪些关键路径，并提供相应的自动化或截图证据。

### Phase 1：清理 CSS 死代码并建立样式所有权（P0）

第一步先处理没有进入构建图的三个文件：

- `layout-fixes.css`
- `responsive-layout.css`
- `slider-responsive.css`

确认没有运行时动态加载后删除，并运行 build、Stylelint 和关键页面视觉回归。随后逐条整理仍被加载的样式：

1. 设计 token、reset、全局可访问性规则留在 `src/index.css`。
2. 只影响一个组件的规则回到组件附近；简单状态优先使用 Tailwind，复杂伪元素和布局算法保留 CSS。
3. 真正跨页面的触屏行为集中到边界清晰的 platform CSS，但避免在 `html`、`body`、所有 `button` 或所有输入框上施加不必要限制。
4. 为每一处 `!important` 记录原因，先禁止新增，再逐步消除由层叠冲突产生的用法。
5. 合并重复的 reduced-motion、high-contrast 和移动断点规则，保留 RSIP canvas 等明确需要手势接管的例外。

验收标准：

- 构建图中不存在未引用 CSS 文件。
- 全局元素选择器均有明确理由和回归覆盖。
- `!important` 数量不增长，并在报告中给出前后差异。
- 关键页面在四类验证环境中无视觉和手势回归。

### Phase 2：统一 Dialog/Modal 可访问性契约（P0）

先做一个 1–2 天的 spike，只比较两种实现：

- 基于现有代码抽取内部 `DialogShell`。
- 仅引入 Radix Dialog primitive，并在其上封装 Momentum 自己的视觉层。

统一契约必须覆盖：

- Portal、遮罩层和层级策略。
- 初始焦点、Tab/Shift+Tab 陷阱、Escape 关闭和关闭后的焦点恢复。
- 背景 inert、页面滚动锁定和嵌套确认框。
- `aria-labelledby`、可选 `aria-describedby`、错误/成功状态播报。
- destructive action 的安全默认焦点。
- reduced motion、移动端可用高度和软键盘场景。

迁移顺序优先选择当前行为最分散的 Import/Export、Task Completion、Rule Selection 和 Recycle Bin，再迁移已有 focus trap 的 Account、Betting 和 Confirmation。

验收标准：每个对话框都有键盘打开/操作/关闭测试；焦点不会逃到背景，关闭后回到触发元素，嵌套确认框不会同时响应父层 Escape。

### Phase 3：建立明确的视觉方向并修复关键流程层级（P1）

建议把 Momentum 的方向定为“克制的专注工具 + 有节制的游戏化反馈”：工作流保持安静清晰，宠物、下注和完成反馈才承担趣味性。当前 `bento-card`、`glass-panel`、大圆角、阴影和渐变在多个页面重复使用，容易让所有信息看起来同等重要。

以三个流程做 pilot，而不是全站换肤：

1. **Dashboard**：让当前任务和主操作在 2 秒内可识别；次要统计和管理入口降级。
2. **Chain Editor**：减少卡片套卡片和重复左侧彩色边框；用分组、留白和渐进披露表达设置层级。
3. **Focus Mode**：减少装饰和次要操作竞争，强化当前状态、剩余时间和唯一下一步。

同时统一：

- 字号、字重、间距、圆角、阴影和状态色 token。
- 空状态、加载、错误、成功和 disabled 状态。
- 中英文长文案下的布局，以及不依赖颜色单独传达状态。
- 动画只用于状态变化，并完整尊重 reduced motion。

验收标准：pilot 页面通过键盘、触屏、暗色、高对比度和长文案检查；相同层级不再出现多套视觉语法；主要动作不与装饰元素竞争。

### Phase 4：类型安全的 i18n 渐进迁移（P1）

1. 从英文基准字典推导 `TranslationKey`，让中文词典 `satisfies Record<TranslationKey, string>`。
2. 将 `t` 收紧为 `t(key: TranslationKey, params?)`，保留 `tr` 作为带废弃标记的兼容 API。
3. 建立 key 清单和检查脚本：语言 key 必须一致、值非空、禁止新增 `tr(...)`、报告未使用 key。
4. 按完整用户流程迁移，而不是按文件机械替换：先 account/settings，再 Dashboard/chain editor，最后 RSIP 与服务层消息。
5. 动态文案使用参数插值；日期、数字和复数规则通过 `Intl` 或明确的 message-format 方案处理。
6. 当 `tr` 调用归零并完成伪语言/长文案测试后，才删除兼容 API 或增加第三语言。

验收标准：新代码无法提交未知 key 或新的双参数翻译；每次迁移都同时覆盖该流程的中英文行为测试。

### Phase 5：按职责削减真实的大文件基线（P1/P2）

不要单独开“删 8 行”的 PR。把拆分绑定到上述工作：

- Dialog 统一时处理 `ImportUnitsModal.tsx`。
- Chain Editor 视觉层级调整时，拆出 `MainChainSettingsSection.tsx` 中可独立测试的规则或子区块。
- AppShell 导航/视图边界调整时，再拆 `AppShellView.tsx` 的路由级渲染分支。

验收标准：`npm run quality:large-files` 持续通过，ratchet 基线只缩不涨；拆分后组件 props、认知复杂度或重复代码至少有一项可量化改善。

### Phase 6：只优化被证实的渲染热点（P2）

- 用 React Profiler 检查 Dashboard、Chain Editor 和 RSIP 更新时的 commit 次数与耗时。
- 为怀疑存在额外渲染的组件加 render-count 测试或可复现 benchmark。
- 只有证据指向 selector 时，才比较原子 selector、合并 selector 和派生 view model。
- 只有 chains 与 RSIP 的更新生命周期确实独立、且收益覆盖同步成本时，才考虑拆 store。

验收标准：优化 PR 提供优化前后相同场景的数据；没有测量结果就不做 store 重构。

### Phase 7：Tailwind v4 独立升级 spike（P3）

前置条件：CSS 死代码和全局覆盖已经收敛，Web/Tauri 浏览器基线已确认。

在独立分支执行官方升级工具并评估：

- 冷构建、增量构建和生产 CSS 大小的前后数据。
- `src/index.css` 的 `@tailwind`/`@layer`/`@apply` 迁移结果。
- `tailwind.config.js` 中颜色、字体、动画和自定义 screen 的迁移。
- Preflight、ring、shadow、outline、important modifier 和暗色模式的视觉差异。
- Web 与目标 Tauri WebView 的关键流程回归。

只有测量收益明确、浏览器兼容且回归成本可控时才合并。不要把“Vite 7”或“可能减小 Tauri 包体”作为升级理由。

## 推荐交付顺序

1. PR A：回归基线 + 删除 3 个未导入 CSS 文件。
2. PR B：Dialog spike 和统一契约测试。
3. PR C：迁移高风险 Dialog，并收敛相关全局 CSS。
4. PR D：Dashboard / Chain Editor / Focus Mode 视觉层级 pilot。
5. PR E：i18n 类型化、禁止新增 `tr`，再按流程渐进迁移。
6. PR F：结合上述改动缩减 large-file ratchet 基线。
7. PR G：有 profiler 证据后再做渲染优化。
8. PR H：Tailwind v4 独立 spike，单独决定是否升级。

## 明确不做

- 不一次性内联或合并所有 CSS。
- 不一次性替换全部 `tr(...)`。
- 不因为文件超过 300 行几行就做无职责收益的拆分。
- 不直接引入整套 shadcn/ui；先决定 primitive 契约和视觉所有权。
- 不在没有 profiler 数据时拆 Zustand store。
- 不把 Tailwind 大版本升级与 CSS 重构放在同一个 PR。
