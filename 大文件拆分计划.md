# 大文件拆分/重构计划

## 目标
将超过 300 行的"屎山"文件拆分为职责单一、易于维护的小模块，遵循项目的 KISS/YAGNI/DRY/SOLID 原则。

---

## 高优先级文件（Phase 1-3）

### 1. `src/types/index.ts` (512行) - Phase 1
**问题**: 所有核心类型混在一个文件中

**拆分方案**:
```
src/types/
├── index.ts              (~50行) - 仅 re-export
├── chain.ts              (~100行) - Chain, ChainType, ChainDraft, ChainTreeNode, DeletedChain
├── session.ts            (~60行) - ScheduledSession, ActiveSession, CompletionHistory
├── rsip.ts               (~80行) - RSIPNode, RSIPTreeNode, RSIPMeta
├── exception-rule.ts     (~120行) - ExceptionRule, ExceptionRuleType, RuleUsageRecord
├── exception-rule-error.ts (~180行) - ExceptionRuleError enum, Exception classes
└── app-state.ts          (~30行) - ViewState, AppState
```

**步骤**:
1. 创建 `chain.ts` - 提取 Chain 相关类型（第1-98行）
2. 创建 `session.ts` - 提取 Session 相关类型
3. 创建 `rsip.ts` - 提取 RSIP 相关类型
4. 创建 `exception-rule.ts` - 提取规则接口
5. 创建 `exception-rule-error.ts` - 提取错误枚举和异常类
6. 创建 `app-state.ts` - 提取应用状态类型
7. 更新 `index.ts` 为纯 re-export

---

### 2. `src/infra/storage/supabase/chains.ts` (442行) - Phase 2
**问题**: CRUD、错误处理、schema兼容性回退混杂

**拆分方案**:
```
src/infra/storage/supabase/chains/
├── index.ts              (~30行) - Re-exports
├── chainQueries.ts       (~120行) - getChains, getActiveChains, getDeletedChains
├── chainMutations.ts     (~150行) - saveChains, softDeleteChain, restoreChain
├── chainCleanup.ts       (~50行) - cleanupExpiredDeletedChains
└── chainErrorHandling.ts (~80行) - formatDbError, isMissingDeletedAtColumnError, findChainAndChildren
```

**步骤**:
1. 创建 `chains/chainErrorHandling.ts` - 提取错误处理函数（第11-56行）
2. 创建 `chains/chainQueries.ts` - 提取查询函数
3. 创建 `chains/chainMutations.ts` - 提取写入函数
4. 创建 `chains/chainCleanup.ts` - 提取清理函数
5. 创建 `chains/index.ts` - re-export 所有函数
6. 更新原 `chains.ts` 为 re-export（向后兼容）

---

### 3. `src/utils/chainTree.ts` (412行) - Phase 2
**问题**: 树构建、验证、遍历、进度计算混杂

**拆分方案**:
```
src/utils/chain-tree/
├── index.ts              (~30行) - Re-exports
├── treeBuilder.ts        (~120行) - validateChainData, buildChainTree
├── treeTraversal.ts      (~60行) - getTopLevelChains
├── groupProgress.ts      (~100行) - getGroupProgress, getGroupUnitProgress, getNextUnitInGroup
├── groupOperations.ts    (~80行) - incrementGroupCompletionCount, resetGroupTaskProgress
└── chainTypeConfig.ts    (~50行) - getChainTypeConfig
```

**步骤**:
1. 创建 `chain-tree/treeBuilder.ts` - 提取 validateChainData, buildChainTree
2. 创建 `chain-tree/treeTraversal.ts` - 提取遍历工具
3. 创建 `chain-tree/groupProgress.ts` - 提取进度计算
4. 创建 `chain-tree/groupOperations.ts` - 提取群组操作
5. 创建 `chain-tree/chainTypeConfig.ts` - 提取类型配置
6. 创建 `chain-tree/index.ts` - re-export
7. 更新原 `chainTree.ts` 为 re-export

---

### 4. `src/utils/ruleSearchOptimizer.ts` (420行) - Phase 3
**问题**: 索引、缓存、评分、建议混杂（已有部分拆分）

**拆分方案** (补充现有 `rule-search-optimizer/`):
```
src/utils/rule-search-optimizer/
├── index.ts              (existing)
├── types.ts              (existing)
├── patterns.ts           (existing)
├── pinyin.ts             (existing)
├── scoring.ts            (existing)
├── searchSuggestions.ts  (existing)
├── RuleSearchIndex.ts    (~100行) - NEW: 索引构建
├── RuleSearchCache.ts    (~60行) - NEW: 缓存管理
└── RuleDuplicateDetector.ts (~50行) - NEW: 重复检测
```

**步骤**:
1. 创建 `RuleSearchIndex.ts` - 提取 updateIndex, indexKeyPrefixes, indexKeyBigrams
2. 创建 `RuleSearchCache.ts` - 提取 cacheSearchResult, recordSearch, getHistorySuggestions
3. 创建 `RuleDuplicateDetector.ts` - 提取 detectDuplicates, generateNameSuggestions
4. 精简主类 `RuleSearchOptimizer.ts` 为编排器

---

### 5. `src/components/rule-manager/RuleManagerViewContainer.tsx` (352行) - Phase 3
**问题**: Container 职责过多

**拆分方案**:
```
src/components/rule-manager/
├── RuleManagerViewContainer.tsx  (~150行) - 精简后的编排器
├── RuleManagerViewView.tsx       (existing)
└── hooks/
    ├── useRuleManagerState.ts    (~80行) - 核心状态
    ├── useRuleFiltering.ts       (~50行) - 过滤/排序
    ├── useRuleForm.ts            (~60行) - 表单状态
    └── useRuleCRUD.ts            (~100行) - CRUD 操作
```

---

## 中优先级文件（Phase 4-5）

| 文件 | 行数 | 拆分方向 |
|------|------|----------|
| `SystemHealthService.ts` | 432 | 按检查器类型拆分 |
| `EnhancedRuleValidationService.ts` | 422 | 按验证器类型拆分 |
| `performanceMonitor.ts` | 405 | 按观察器类型拆分 |
| `GroupCard.tsx` | 407 | 提取子组件 + hooks |
| `GroupViewView.tsx` | 405 | 提取子组件 |

---

## 执行顺序

```
Phase 1: src/types/index.ts (基础，无依赖)
    ↓
Phase 2: chains.ts + chainTree.ts (基础设施层)
    ↓
Phase 3: ruleSearchOptimizer.ts + RuleManagerViewContainer.tsx (工具+组件)
    ↓
Phase 4: Services (SystemHealthService, EnhancedRuleValidationService)
    ↓
Phase 5: Components (GroupCard, GroupViewView)
```

---

## 验证方法

每个文件重构后执行：

```bash
# 1. 类型检查
npm run typecheck

# 2. 循环依赖检测
npm run quality:circular

# 3. 未使用导出检测
npm run quality:knip
npm run quality:ts-prune

# 4. 测试
npm test

# 5. Lint
npm run lint
```

---

## 命名约定

遵循项目现有约定：
- `*Service.ts` - 业务逻辑
- `*Manager.ts` - 状态/生命周期协调
- `*Cache.ts` - 缓存逻辑
- `*Checker.ts` - 验证检查
- `*Handler.ts` - 事件处理
- `types.ts` - 类型定义
- `index.ts` - Re-exports

---

## 关键文件路径

- `src/types/index.ts` - 类型定义入口
- `src/infra/storage/supabase/chains.ts` - Chain 存储层
- `src/utils/chainTree.ts` - 链树工具
- `src/utils/ruleSearchOptimizer.ts` - 规则搜索
- `src/components/rule-manager/RuleManagerViewContainer.tsx` - 规则管理容器
