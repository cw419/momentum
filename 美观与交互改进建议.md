美观与交互改进建议

  1. Focus Mode：让专注时间有存在感

  这是整个应用最重要的屏幕，用户的真实时间在这里流逝，但目前视觉上几乎是静止的：

  - 计时器只是一个数字。可以在数字外围加一个随时间收缩的呼吸圆环（SVG stroke-dashoffset 动画），比细进度条更有仪式感
  - 暂停状态和运行状态外观几乎相同。暂停时整个界面应该明显变暗或模糊，让用户感知到"时间停了"
  - 红色中断按钮固定在右下角，心理上等于给用户一个随时可以按的逃跑键。更好的方案是长按才触发，或隐藏在侧滑手势后面，不让
  它一直占据视野
  - 完成任务时没有庆祝。TaskCompletionDialog 弹出后体验就结束了——连纸屑都没有。条纹达到 7 天、30
  天时更应该有明显的里程碑动效

  2. ChainCard：条纹数字没有情感

  卡片上两个蓝绿色 stat box 并排显示 #0 对新用户毫无激励作用，看起来像 bug 而不是邀请。可以：

  - #0 时换成"开始你的第一链"+ 小火花图标，而不是空数字
  - 条纹达到特定里程碑（7、30、100）时给数字加火焰光晕或颤动动画
  - 不同任务类型（assault/recon/special_ops 等）目前只靠一个小8px icon
  区分，颜色主题可以更鲜明——专注时整个背景渐变都换色，而不只是图标

  3. 视图切换没有过渡

  在 dashboard → focus → detail 之间切换目前是：Suspense spinner → 内容突然出现。两个方案：

  - CSS View Transitions API（Chrome/Tauri WebView 已支持），只需几行 CSS 和document.startViewTransition()
  就能实现平滑的共享元素过渡，从卡片到全屏专注界面的放大动画
  - 退而求其次，至少加一个 opacity: 0 → 1 的入场动画，比裸切更顺眼

  4. "链"这个隐喻在视觉上消失了

  应用叫 Momentum，核心概念是"链"（streak chain），但界面上看不到任何链的形象。这是一个被浪费的品牌机会：

  - 条纹数字旁边可以用小圆点连成的链条图案（类似 GitHub contribution grid，但更线性）直观展示近期完成记录
  - GroupCard 里的子任务可以用细线串联，而不是普通列表，直观表达"这些任务是一个整体"
  - 完成界面可以出现一个链条增加新环节的动画

  5. Pet Widget 的潜力远未发挥

  宠物系统是整个 app 最有创意的差异点，但目前只是角落里一个会弹跳的 emoji。改进方向：

  - 专注模式进行中时，宠物可以出现在画面边缘小声"陪跑"（比如一个小窗，只有宠物脸）
  - 完成任务时宠物应该明显反应（跳跃动画、心形飘出）而不是保持原状
  -阶段晋升（蛋→雏鸟→...）应该是一个有仪式感的事件，而不是悄悄改变的emoji

  6. Loading 体验：用骨架屏替代 spinner

  当前加载是居中转圈。换成与 ChainCard 形状吻合的骨架屏（灰色占位矩形）会让加载感觉更快，也更精致，开发量不大（纯 CSS +
  Tailwind animate-pulse）。

  7. 震动与声音反馈

  @tauri-apps/plugin-haptics 已经在依赖里，但估计没有在关键时刻用起来：

  - 任务完成：成功震动模式
  - 中断任务：警告震动模式
  - 里程碑达成：特殊震动序列

  Web 端可以加一个可选的轻微完成音效（UI sound，默认关，可在设置里开启），单一音调足够，不需要音乐库。

  8. Dashboard：任务墙缺乏优先级感

  多条链并排显示时是一个均质的网格，没有任何"今天应该先做哪个"的指引。可以在顶部增加一个"今日推荐"区域：根据触发器时间、
  上次完成时间、条纹危险程度（快断了）自动排序，让用户打开 app 时第一眼知道该点哪张卡。

  ---
  一句话总结：目前的设计基础是扎实且现代的（bento card、glassmorphism、dark
  mode），但还停在"把数据展示出来"这个层面。真正能让用户感受到 Momentum
  不同的，是让专注屏幕有仪式感、让条纹数字有情感重量、让那条"链"在视觉上真实存在。这些改动大部分不需要引入新库，用 CSS
  动画和现有 Tauri 插件就能实现大半。